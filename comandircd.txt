COMANDI da ricordare

terminale
pwd: path
cd: sposta
ls: lista
mkdir: crea directory

open: apri in Text edit
atom: apre atom


GIT (https://git-scm.com/book/en/v2)
git status

git add
git rm (eliminare file) {non serve add, passa a commit}
git mv (rinominare file) {non serve add, passa a commit}


git commit -m "messaggio"

usa .gitignore per risorse da non considerare in ambiente Git

git diff per prendere visione di quali file sono in working or stage (--cached) context // granularità di parola per differenze (--word-diff)
